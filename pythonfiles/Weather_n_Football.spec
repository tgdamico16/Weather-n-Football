# -*- mode: python -*-

block_cipher = None


a = Analysis(['E:\\school\\2018-19\\caps\\A Foot in the Door\\pythonfiles\\footinthedoor.py'],
             pathex=['E:\\school\\2018-19\\caps\\A Foot in the Door\\pythonfiles'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='Weather_n_Football',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='Weather_n_Football')
